import React from 'react';
import styled from 'styled-components';

const ColumnGroup = styled.section`
display: flex;
gap:20px;
flex-direction: column;
justify-content: space-evenly;
align-items stretch;
margin:0 20px;
`

export default ColumnGroup;
